{
  "name": "olimpiada-grupo-a",
  "version": "1.0.0",
  "description": "App fullstack para despliegue en Replit",
  "scripts": {
    "clean-install": "rm -rf backend/node_modules backend/package-lock.json && cd backend     && npm install && cd ../frontend && rm -rf node_modules package-lock.json && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node index.js",
    "reset": "npm run clean-install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}